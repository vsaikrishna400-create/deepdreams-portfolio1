# 🔄 Restore & Polish

**I have reset the project to exactly when the videos were working perfect.**

1.  **Videos:** Back to the "Standard File" set up (lowercase names: `shiva.mp4`, `temple.mp4`) which you confirmed was working.
2.  **Navigation Title (Small Text):** I kept the original size and font. I ONLY changed the color to Solid Gold to remove the black spots.
3.  **Hero Title (Big Text):** Back to the version that had no black spots.

## ⚡ Restore Live Site

Run this command to push this verified state:

```powershell
git push origin main --force
```

*(We use `--force` because we went back in time to the good version)*
