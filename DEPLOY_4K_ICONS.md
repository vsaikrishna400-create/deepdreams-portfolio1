# 🦅 4K Enhanced Icons

**I have maximized the quality and size of your icons.**

1.  **Hero Icon (Middle):**
    - **Size:** MASSIVELY increased. It's now huge and commanding (up to `w-80` on desktops).
    - **Quality:** Added a "Depth Glow" shadow to make it pop like a 4K asset.
    
2.  **Navigation Icon (Left):**
    - **Size:** Increased from `w-10` to `w-16` (Much larger).
    - **Quality:** Turned off compression (`unoptimized`) so it renders pixel-perfect.

## ⚡ Deploy
```powershell
git add .
git commit -m "Enhance icon sizes and enable unoptimized rendering for quality"
git push origin main
```
